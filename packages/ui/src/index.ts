/**
 * @noema/ui — punto de entrada principal.
 *
 * Exporta tokens, helpers y (más adelante) componentes compartidos.
 * Los componentes de RN viven en `./mobile/`, los de web en `./web/` —
 * por ahora solo exponemos lo agnóstico de plataforma.
 */

export * from './tokens';
export * from './theme';
